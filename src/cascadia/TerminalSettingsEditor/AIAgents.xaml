<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<Page x:Class="Microsoft.Terminal.Settings.Editor.AIAgents"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Microsoft.Terminal.Settings.Editor"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:mtu="using:Microsoft.Terminal.UI"
      xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
      mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="CommonResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <x:Double x:Key="CustomProviderInputWidth">496</x:Double>

            <DataTemplate x:Key="AgentEntryTemplate"
                          x:DataType="local:AgentEntry">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               Text="{x:Bind DisplayLabel}"
                               Opacity="{x:Bind local:AIAgents.InstalledOpacity(IsInstalled)}" />
                    <Button Grid.Column="1"
                            x:Uid="Actions_EditButton"
                            Margin="8,0,0,0"
                            AutomationProperties.AutomationId="CustomAgentEditButton"
                            AutomationProperties.Name="{Binding Path=(ToolTipService.ToolTip), RelativeSource={RelativeSource Self}}"
                            Click="CustomAgentEdit_Click"
                            Loaded="CustomAgentAction_Loaded"
                            Style="{StaticResource ExtraSmallButtonStyle}"
                            Visibility="Collapsed">
                        <FontIcon FontSize="12"
                                  Glyph="&#xE70F;" />
                    </Button>
                    <Button Grid.Column="2"
                            x:Uid="AIAgents_CustomAgentRemove"
                            Margin="4,0,0,0"
                            AutomationProperties.AutomationId="CustomAgentRemoveButton"
                            Click="{x:Bind Remove}"
                            Loaded="CustomAgentAction_Loaded"
                            Style="{StaticResource ExtraSmallButtonStyle}"
                            Visibility="Collapsed">
                        <FontIcon FontSize="12"
                                  Glyph="&#xE711;" />
                    </Button>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="EnumComboBoxTemplate"
                          x:DataType="local:EnumEntry">
                <TextBlock Text="{x:Bind EnumName, Mode=OneWay}" />
            </DataTemplate>

            <DataTemplate x:Key="AutoErrorHandlingComboBoxTemplate"
                          x:DataType="local:EnumEntry">
                <TextBlock Text="{x:Bind EnumName, Mode=OneWay}"
                           TextWrapping="WrapWholeWords" />
            </DataTemplate>

            <DataTemplate x:Key="AcpModelEntryTemplate"
                          x:DataType="local:AcpModelEntry">
                <TextBlock Text="{x:Bind DisplayName}" />
            </DataTemplate>

            <Style x:Key="CustomModelProviderRowStyle"
                   TargetType="Border">
                <Setter Property="MinHeight" Value="64" />
                <Setter Property="Padding" Value="0,12" />
            </Style>

            <Style x:Key="InlineSettingsLinkStyle"
                   BasedOn="{StaticResource DefaultHyperlinkButtonStyle}"
                   TargetType="HyperlinkButton">
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style x:Key="AutoErrorHandlingSettingContainerStyle"
                   TargetType="local:SettingContainer">
                <Setter Property="Margin" Value="0,4,0,0" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="MaxWidth" Value="{StaticResource StandardControlMaxWidth}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="local:SettingContainer">
                            <Grid Style="{StaticResource NonExpanderGrid}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Margin="0,12,0,12"
                                                  Content="{TemplateBinding Header}" />
                                <ContentPresenter Grid.Column="1"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="AgentCliSettingContainerStyle"
                   BasedOn="{StaticResource DefaultSettingContainer}"
                   TargetType="local:SettingContainer">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="local:SettingContainer">
                            <Grid AutomationProperties.Name="{TemplateBinding Header}"
                                  Style="{StaticResource NonExpanderGrid}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Style="{StaticResource StackPanelInExpanderStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Style="{StaticResource SettingsPageItemHeaderStyle}"
                                                   Text="{TemplateBinding Header}" />
                                        <Button x:Name="ResetButton"
                                                Style="{StaticResource SettingContainerResetButtonStyle}">
                                            <FontIcon Glyph="&#xE845;"
                                                      Style="{StaticResource SettingContainerFontIconStyle}" />
                                        </Button>
                                    </StackPanel>
                                    <RichTextBlock x:Name="HelpTextBlock"
                                                   Margin="0,0,24,0"
                                                   AutomationProperties.FullDescription="{TemplateBinding HelpText}"
                                                   FontFamily="Segoe UI, Segoe Fluent Icons, Segoe MDL2 Assets"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource SubgroupHeaderBrush}"
                                                   IsTextSelectionEnabled="False"
                                                   Loaded="DelegateAgentHelp_Loaded"
                                                   Tag="{TemplateBinding HelpText}"
                                                   TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <ContentPresenter Grid.Column="1"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Page.Resources>

    <StackPanel>
        <StackPanel Style="{StaticResource SettingsStackStyle}">

            <!--  Bounded inline controls avoid the text renderer's nearest-link hit testing.  -->
            <RichTextBlock x:Name="PageSubtitleText"
                           Margin="0,0,0,16"
                           IsTextSelectionEnabled="False"
                           TextWrapping="Wrap">
                <Paragraph>
                    <Run x:Name="PageSubtitlePrefix"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><Run Text=" " /><InlineUIContainer>
                        <HyperlinkButton x:Name="PageSubtitlePrivacyLinkButton"
                                         NavigateUri="https://aka.ms/intelligent-terminal-privacy"
                                         Style="{StaticResource InlineSettingsLinkStyle}">
                            <TextBlock x:Name="PageSubtitlePrivacyLink"
                                       Loaded="InlineLinkText_Loaded"
                                       SizeChanged="InlineLinkText_SizeChanged"
                                       TextDecorations="Underline" />
                        </HyperlinkButton>
                    </InlineUIContainer>
                </Paragraph>
            </RichTextBlock>

            <!--  ═══ Agent pane group ═══
                  First sub-header on the page: override the style's 32px
                  top margin to 0 so the page subtitle's bottom margin is
                  the only gap above the header. Otherwise, the page
                  subtitle + sub-header margins stack to ~48px and look
                  visibly larger than every other settings page (which
                  doesn't render a page subtitle).  -->
            <TextBlock x:Uid="AIAgents_AcpGroupHeader"
                       Style="{StaticResource TextBlockSubHeaderStyle}"
                       Margin="0,0,0,4" />

            <!--  Agent (ACP CLI) — hand-built Grid mimicking the NonExpanderGrid chrome of
                  local:SettingContainer, so the description TextBlock can host an inline Hyperlink.  -->
            <Grid x:Name="AcpAgent"
                  Style="{StaticResource NonExpanderGrid}"
                  MaxWidth="{StaticResource StandardControlMaxWidth}"
                  Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"
                            Style="{StaticResource StackPanelInExpanderStyle}">
                    <TextBlock x:Name="AcpAgentHeaderText"
                               Style="{StaticResource SettingsPageItemHeaderStyle}" />
                    <!--  Run.Text values populated from code-behind by splitting on "ACP".  -->
                    <TextBlock x:Name="AcpAgentDescriptionText"
                               Style="{StaticResource SettingsPageItemDescriptionStyle}">
                        <Run x:Name="AcpAgentDescriptionBefore" />
                        <Hyperlink NavigateUri="https://aka.ms/intelligent-terminal-acpref">
                            <Run x:Name="AcpAgentDescriptionAcpToken" />
                        </Hyperlink>
                        <Run x:Name="AcpAgentDescriptionAfter" />
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,12,0,12">
                    <ComboBox x:Name="AcpAgentComboBox"
                              AutomationProperties.AccessibilityView="Content"
                              ItemTemplate="{StaticResource AgentEntryTemplate}"
                              ItemsSource="{x:Bind ViewModel.AcpAgentList, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.CurrentAcpAgent, Mode=TwoWay}"
                              Style="{StaticResource ComboBoxSettingStyle}" />
                    <TextBlock x:Uid="AIAgents_PolicyLocked"
                               Visibility="{x:Bind ViewModel.IsAgentPolicyLocked, Mode=OneWay}"
                               Opacity="0.6"
                               Margin="0,4,0,0" />
                    <!--  Editable panel (adding new or editing existing)  -->
                    <StackPanel Orientation="Vertical"
                                Visibility="{x:Bind ViewModel.IsAddingCustomAcpAgent, Mode=OneWay}"
                                Margin="0,8,0,0"
                                Spacing="8">
                        <TextBox x:Name="CustomAcpCommandBox"
                                 Text="{x:Bind ViewModel.CustomAcpCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderForeground="{ThemeResource TextFillColorTertiaryBrush}"
                                 PlaceholderText="my-agent --acp --stdio"
                                 Style="{StaticResource TextBoxSettingStyle}" />
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Spacing="8">
                            <Button x:Name="CustomAcpAgentSaveButton" Content="Save" Click="{x:Bind ViewModel.SaveCustomAcpAgent}" Style="{StaticResource AccentButtonStyle}" />
                            <Button x:Name="CustomAcpAgentCancelButton" Content="Cancel" Click="{x:Bind ViewModel.CancelCustomAcpAgent}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
             </Grid>

            <!--  Model picker — shown for every selected agent (built-in
                  or custom) so a stale acpModel value is always visible
                  and clearable. HasAcpModelList picks between the
                  dropdown (helper published available_models) and the
                  free-form textbox fallback.  -->
            <local:SettingContainer x:Name="AcpModel"
                                    x:Uid="AIAgents_AcpModel"
                                    Visibility="{x:Bind ViewModel.ShowAcpModel, Mode=OneWay}">
                <Grid>
                    <!--  Dropdown: shown when wta has pushed a model list via agent_status  -->
                    <ComboBox AutomationProperties.AccessibilityView="Content"
                              ItemsSource="{x:Bind ViewModel.AcpModelList, Mode=OneWay}"
                              ItemTemplate="{StaticResource AcpModelEntryTemplate}"
                              SelectedItem="{x:Bind ViewModel.CurrentAcpModelEntry, Mode=TwoWay}"
                              PlaceholderText="Default"
                              Style="{StaticResource ComboBoxSettingStyle}"
                              Visibility="{x:Bind ViewModel.HasAcpModelList, Mode=OneWay}" />
                    <!--  Free-form fallback: agent pane never connected, custom agent, or agent didn't advertise models  -->
                    <TextBox Text="{x:Bind ViewModel.AcpModel, Mode=TwoWay}"
                             PlaceholderText="Default"
                             Style="{StaticResource TextBoxSettingStyle}"
                             Visibility="{x:Bind ViewModel.ShowAcpModelTextBox, Mode=OneWay}" />
                </Grid>
            </local:SettingContainer>

            <muxc:Expander x:Name="CustomModelProvidersExpander"
                           Margin="0,4,0,0"
                           Padding="16,0"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Stretch"
                           IsExpanded="{x:Bind ViewModel.IsCustomModelProvidersExpanded, Mode=TwoWay}">
                <muxc:Expander.Header>
                    <Grid MinHeight="64">
                        <StackPanel Style="{StaticResource StackPanelInExpanderStyle}">
                            <TextBlock x:Name="CustomModelsHeaderText"
                                       Style="{StaticResource SettingsPageItemHeaderStyle}" />
                            <RichTextBlock x:Name="CustomModelsDescriptionText"
                                           Margin="0,0,24,0"
                                           FontFamily="Segoe UI, Segoe Fluent Icons, Segoe MDL2 Assets"
                                           FontSize="12"
                                           Foreground="{ThemeResource SubgroupHeaderBrush}"
                                           IsTextSelectionEnabled="False"
                                           LineHeight="16"
                                           TextWrapping="WrapWholeWords">
                                <Paragraph>
                                    <Run x:Name="CustomModelsCaptionPrefix" /><InlineUIContainer>
                                        <HyperlinkButton x:Name="CustomModelsLearnMoreButton"
                                                         FontSize="12"
                                                         NavigateUri="https://github.com/microsoft/intelligent-terminal#custom-providers-byok"
                                                         Style="{StaticResource InlineSettingsLinkStyle}">
                                            <TextBlock x:Name="CustomModelsCaptionLink"
                                                       Loaded="InlineLinkText_Loaded"
                                                       SizeChanged="InlineLinkText_SizeChanged"
                                                       TextDecorations="Underline" />
                                        </HyperlinkButton>
                                    </InlineUIContainer>
                                </Paragraph>
                            </RichTextBlock>
                        </StackPanel>
                    </Grid>
                </muxc:Expander.Header>
                <StackPanel>
                    <TextBlock Margin="0,12,0,0"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Text="{x:Bind ViewModel.CustomModelProviderUnsupportedMessage, Mode=OneWay}"
                               TextWrapping="Wrap"
                               Visibility="{x:Bind mtu:Converters.StringNotEmptyToVisibility(ViewModel.CustomModelProviderUnsupportedMessage), Mode=OneWay}" />
                    <ItemsControl ItemsSource="{x:Bind ViewModel.CustomModelProviders, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="local:CustomModelProviderEntry">
                                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="0,0,0,1"
                                        Style="{StaticResource CustomModelProviderRowStyle}">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center" Spacing="2">
                                            <TextBlock Text="{x:Bind ModelsDisplayText, Mode=OneWay}"
                                                       TextWrapping="Wrap" />
                                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                       Opacity="0.6"
                                                       Text="{x:Bind BaseUrl, Mode=OneWay}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <StackPanel Orientation="Horizontal"
                                                        Spacing="6"
                                                        Visibility="{x:Bind IsApiKeyMissing, Mode=OneWay}">
                                                <FontIcon FontSize="12"
                                                          Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                          Glyph="&#xE814;" />
                                                <TextBlock x:Uid="AIAgents_CustomProviderApiKeyMissing"
                                                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                        <Button Grid.Column="1"
                                                x:Uid="AIAgents_CustomProviderRemove"
                                                VerticalAlignment="Center"
                                                Click="{x:Bind Remove}"
                                                MinWidth="120" />
                                        <muxc:InfoBar Grid.Row="1"
                                                      Grid.ColumnSpan="2"
                                                      Margin="0,12,0,0"
                                                      IsClosable="False"
                                                      IsOpen="{x:Bind HasRemovalError, Mode=OneWay}"
                                                      Message="{x:Bind RemovalErrorMessage, Mode=OneWay}"
                                                      Severity="Error"
                                                      Visibility="{x:Bind HasRemovalError, Mode=OneWay}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Style="{StaticResource CustomModelProviderRowStyle}"
                            Visibility="{x:Bind mtu:Converters.InvertedBooleanToVisibility(ViewModel.IsAddingCustomModelProvider), Mode=OneWay}">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="CustomProviderAddButton"
                                    Grid.Column="1"
                                    x:Uid="AIAgents_CustomProviderAdd"
                                    Click="{x:Bind ViewModel.AddCustomModelProvider}"
                                    MinWidth="120"
                                    Style="{StaticResource AccentButtonStyle}" />
                        </Grid>
                    </Border>

                    <Border Padding="0,16"
                            Visibility="{x:Bind ViewModel.IsAddingCustomModelProvider, Mode=OneWay}">
                        <StackPanel x:Name="CustomProviderForm"
                                    SizeChanged="CustomProviderForm_SizeChanged"
                                    Spacing="12">
                            <TextBlock x:Uid="AIAgents_CustomProviderAddHeader"
                                       FontWeight="SemiBold"
                                       Style="{StaticResource SettingsPageItemHeaderStyle}" />
                            <StackPanel Spacing="4">
                                <TextBox x:Name="CustomProviderBaseUrlBox"
                                         x:Uid="AIAgents_CustomProviderBaseUrl"
                                         Width="{StaticResource CustomProviderInputWidth}"
                                         MaxWidth="{StaticResource CustomProviderInputWidth}"
                                         AutomationProperties.HelpText="{Binding Text, ElementName=CustomProviderBaseUrlHint}"
                                         PlaceholderForeground="{ThemeResource TextFillColorTertiaryBrush}"
                                         Style="{StaticResource TextBoxSettingStyle}"
                                         Text="{x:Bind ViewModel.NewCustomModelProviderBaseUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock x:Name="CustomProviderBaseUrlHint"
                                           x:Uid="AIAgents_CustomProviderBaseUrlHint"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <TextBox x:Name="CustomProviderModelIdBox"
                                         x:Uid="AIAgents_CustomModelId"
                                         Width="{StaticResource CustomProviderInputWidth}"
                                         MaxWidth="{StaticResource CustomProviderInputWidth}"
                                         AutomationProperties.HelpText="{Binding Text, ElementName=CustomModelIdHint}"
                                         PlaceholderForeground="{ThemeResource TextFillColorTertiaryBrush}"
                                         Style="{StaticResource TextBoxSettingStyle}"
                                         Text="{x:Bind ViewModel.NewCustomModelId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock x:Name="CustomModelIdHint"
                                           x:Uid="AIAgents_CustomModelIdHint"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <PasswordBox x:Name="CustomProviderApiKeyBox"
                                             x:Uid="AIAgents_CustomProviderApiKey"
                                             Width="{StaticResource CustomProviderInputWidth}"
                                             MinWidth="{StaticResource StandardBoxMinWidth}"
                                             MaxWidth="{StaticResource CustomProviderInputWidth}"
                                             HorizontalAlignment="Left"
                                             AutomationProperties.HelpText="{Binding Text, ElementName=CustomProviderApiKeyHint}"
                                             Password="{x:Bind ViewModel.NewCustomModelProviderApiKey, Mode=TwoWay}" />
                                <TextBlock x:Name="CustomProviderApiKeyHint"
                                           x:Uid="AIAgents_CustomProviderApiKeyHint"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <TextBlock x:Uid="AIAgents_CustomProviderSaveHint"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           TextWrapping="Wrap" />
                                <StackPanel HorizontalAlignment="Right"
                                            Orientation="Horizontal"
                                            Spacing="8">
                                    <Button x:Uid="AIAgents_CustomProviderSave"
                                            IsEnabled="{x:Bind ViewModel.CanSaveCustomModelProvider, Mode=OneWay}"
                                            Click="{x:Bind ViewModel.SaveCustomModelProvider}"
                                            Style="{StaticResource AccentButtonStyle}" />
                                    <Button x:Name="CustomProviderCancelButton"
                                            x:Uid="AIAgents_CustomProviderCancel"
                                            Click="{x:Bind ViewModel.CancelCustomModelProvider}" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </muxc:Expander>

            <!--  Pane position  -->
            <local:SettingContainer x:Name="AgentPanePosition"
                                x:Uid="AIAgents_PanePosition">
                <ComboBox AutomationProperties.AccessibilityView="Content"
                          ItemTemplate="{StaticResource EnumComboBoxTemplate}"
                          ItemsSource="{x:Bind ViewModel.AgentPanePositionList, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.CurrentAgentPanePosition, Mode=TwoWay}"
                          Style="{StaticResource ComboBoxSettingStyle}" />
            </local:SettingContainer>

            <local:SettingContainer x:Name="AutoErrorHandling"
                                    Style="{StaticResource AutoErrorHandlingSettingContainerStyle}">
                <local:SettingContainer.Header>
                    <StackPanel>
                        <TextBlock x:Uid="AIAgents_AutoErrorHandlingTitle"
                                   Style="{StaticResource SettingsPageItemHeaderStyle}" />
                        <TextBlock x:Uid="AIAgents_AutoErrorHandlingDescription"
                                   Style="{StaticResource SettingsPageItemDescriptionStyle}" />
                        <TextBlock x:Uid="AIAgents_PolicyLocked"
                                   Visibility="{x:Bind ViewModel.IsAutoErrorHandlingPolicyRestricted, Mode=OneWay}"
                                   Opacity="0.6"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </local:SettingContainer.Header>
                <Grid>
                    <ComboBox x:Uid="AIAgents_AutoErrorHandlingComboBox"
                              AutomationProperties.AccessibilityView="Content"
                              Width="{StaticResource StandardBoxMinWidth}"
                              ItemTemplate="{StaticResource AutoErrorHandlingComboBoxTemplate}"
                              ItemsSource="{x:Bind ViewModel.AutoErrorHandlingList, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.CurrentAutoErrorHandling, Mode=TwoWay}"
                              Style="{StaticResource ComboBoxSettingStyle}" />
                </Grid>
            </local:SettingContainer>

            <local:SettingContainer x:Name="AgentSessionManagement"
                                    x:Uid="AIAgents_SessionManagement">
                <StackPanel>
                    <ToggleSwitch AutomationProperties.AccessibilityView="Content"
                                  IsOn="{x:Bind ViewModel.AgentSessionManagementEnabled, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.CanConfigureAgentSessionManagement, Mode=OneWay}"
                                  Style="{StaticResource ToggleSwitchInExpanderStyle}" />
                    <TextBlock x:Uid="AIAgents_PolicyLocked"
                               Visibility="{x:Bind ViewModel.IsAgentSessionHooksPolicyLocked, Mode=OneWay}"
                               Opacity="0.6"
                               Margin="0,4,0,0" />
                </StackPanel>
            </local:SettingContainer>

            <local:SettingContainer x:Name="ShowTokenUsageAndCost"
                                    x:Uid="AIAgents_ShowTokenUsageAndCost">
                <ToggleSwitch x:Name="ShowTokenUsageAndCostToggle"
                              AutomationProperties.AccessibilityView="Content"
                              IsOn="{x:Bind ViewModel.ShowTokenUsageAndCost, Mode=TwoWay}"
                              Style="{StaticResource ToggleSwitchInExpanderStyle}" />
            </local:SettingContainer>

            <!--  Default-provider Yolo preference. Policy and provider
                capability decide whether the toggle is editable.  -->
            <local:SettingContainer x:Name="AgentPaneYoloMode"
                                    x:Uid="AIAgents_YoloMode"
                                    Visibility="{x:Bind ViewModel.AgentPaneYoloModeVisibility, Mode=OneWay}">
                <StackPanel>
                    <ToggleSwitch AutomationProperties.AutomationId="AgentPaneYoloModeToggle"
                                  AutomationProperties.AccessibilityView="Content"
                                  IsOn="{x:Bind ViewModel.AgentPaneYoloMode, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.CanEnableAgentPaneYoloMode, Mode=OneWay}"
                                  Style="{StaticResource ToggleSwitchInExpanderStyle}" />
                </StackPanel>
            </local:SettingContainer>
            <muxc:InfoBar x:Uid="AIAgents_YoloGeminiInfo"
                          AutomationProperties.AutomationId="GeminiYoloCompatibilityInfoBar"
                          HorizontalAlignment="Stretch"
                          IsClosable="False"
                          IsOpen="{x:Bind ViewModel.ShowGeminiYoloInfo, Mode=OneWay}"
                          Margin="0,4,0,0"
                          MaxWidth="1000"
                          Severity="Informational"
                          Visibility="{x:Bind ViewModel.ShowGeminiYoloInfo, Mode=OneWay}" />

            <!--  ═══ Command palette group ═══  -->
            <TextBlock x:Uid="AIAgents_DelegateGroupHeader"
                       Style="{StaticResource TextBlockSubHeaderStyle}" />

            <!--  Agent (delegate / on-demand CLI)  -->
            <local:SettingContainer x:Name="DelegateAgent"
                                    x:Uid="AIAgents_DelegateAgent"
                                    Style="{StaticResource AgentCliSettingContainerStyle}">
                <!--  Vertical Margin — same rationale as the AcpAgent
                      container above: when a custom delegate agent is
                      selected the content grows past the SettingContainer's
                      MinHeight=64 and would otherwise hug the card edges.  -->
                <StackPanel Margin="0,12,0,12">
                    <ComboBox x:Name="DelegateAgentComboBox"
                              AutomationProperties.AccessibilityView="Content"
                              ItemTemplate="{StaticResource AgentEntryTemplate}"
                              ItemsSource="{x:Bind ViewModel.DelegateAgentList, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.CurrentDelegateAgent, Mode=TwoWay}"
                              Style="{StaticResource ComboBoxSettingStyle}" />
                    <TextBlock x:Uid="AIAgents_PolicyLocked"
                               Visibility="{x:Bind ViewModel.IsAgentPolicyLocked, Mode=OneWay}"
                               Opacity="0.6"
                               Margin="0,4,0,0" />
                    <StackPanel Orientation="Vertical"
                                Visibility="{x:Bind ViewModel.IsAddingCustomDelegateAgent, Mode=OneWay}"
                                Margin="0,8,0,0"
                                Spacing="8">
                        <TextBox x:Name="CustomDelegateCommandBox"
                                 Text="{x:Bind ViewModel.CustomDelegateCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderForeground="{ThemeResource TextFillColorTertiaryBrush}"
                                 PlaceholderText="my-cli --flag"
                                 Style="{StaticResource TextBoxSettingStyle}" />
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Spacing="8">
                            <Button x:Name="CustomDelegateAgentSaveButton" Content="Save" Click="{x:Bind ViewModel.SaveCustomDelegateAgent}" Style="{StaticResource AccentButtonStyle}" />
                            <Button x:Name="CustomDelegateAgentCancelButton" Content="Cancel" Click="{x:Bind ViewModel.CancelCustomDelegateAgent}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </local:SettingContainer>

        </StackPanel>
    </StackPanel>
</Page>
