<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<UserControl x:Class="TerminalApp.AgentPaneContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:TerminalApp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  ========= Agent bar (Figma: 36px tall, 1px bottom hairline) =========
              Only a bottom hairline: the top edge sits against the terminal
              pane / focused-pane border above, which already provides the
              separation, so a top border would double it up (#348).  -->
        <Border x:Name="AgentBarRoot"
                Grid.Row="0"
                Height="36"
                Background="#FF000000"
                BorderBrush="#26FFFFFF"
                BorderThickness="0,0,0,1">
            <Grid Padding="10,0,0,0">
                <!--  Left cluster: logo + label. History and other actions
                      live on the window-level bottom agent bar.  -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Spacing="4">
                    <!--  Agent logo. Source is a packaged white-filled SVG;
                          the URI is set from code-behind to ms-appx:///AgentIcons/<id>.svg.
                          BitmapIcon + ShowAsMonochrome so the logo takes the
                          theme-aware Foreground set in ApplyThemeColors (#348)
                          instead of staying white on a light bar.  -->
                    <BitmapIcon x:Name="AgentLogo"
                                Width="14"
                                Height="14"
                                ShowAsMonochrome="True" />
                    <TextBlock x:Name="AgentLabelText"
                               VerticalAlignment="Center"
                               FontFamily="Segoe UI Variable, Segoe UI"
                               FontSize="12"
                               LineHeight="16"
                               Foreground="#FFFFFFFF"
                               Text="GitHub Copilot" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  ========= Inner content (the wta TermControl, set in code-behind) =========  -->
        <ContentPresenter x:Name="InnerContent"
                          Grid.Row="1"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" />
    </Grid>
</UserControl>
