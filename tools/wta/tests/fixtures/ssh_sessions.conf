Host fixture
    HostName example.invalid
    EscapeChar ~
    SendEnv *
    SetEnv WTA_MCP_TOKEN=fixture-secret ROUTING=fixture
    ProxyCommand ssh -W %h:%p jump
    IdentityFile ~/.ssh/fixture
